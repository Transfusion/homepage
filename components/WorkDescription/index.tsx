import type { JSX } from "react";
type WorkDescriptionProps = {
    bg_component: JSX.Element,
    title: String,
    desc: String,
    people_impact: String
}

export default function WorkDescription(props: WorkDescriptionProps) {
    return <></>
}